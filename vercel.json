{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx tsc --project packages/shared/tsconfig.json && cd apps/web && npx vite build",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
